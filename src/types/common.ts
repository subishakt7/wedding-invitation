export interface ComponentWithClassName {
  className?: string;
}

export interface WeddingEvent {
  title: string;
  date: string;
  time: string;
  venue: string;
  location?: string;
}

export interface CoupleInfo {
  name: string;
  parents: string;
} 